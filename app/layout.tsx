import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
});

export const metadata: Metadata = {
  title: "Free AI Coloring Page Generator Online | ColorMe",
  description: "Create unique AI coloring pages from photos or text in minutes. Free, no signup, print-ready PNG downloads.",
  openGraph: {
    title: "Free AI Coloring Page Generator Online",
    description: "Turn any photo into a printable coloring page with AI.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col font-sans">{children}</body>
    </html>
  );
}
